### Tests

The test suite below runs some verification of the basic features of the
library.

To build the tests:

```shell
make
```

To run tests:

```shell
./run_tests
```

To clean test build:
```shell
make clean
```
